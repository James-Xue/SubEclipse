# 第4周代码三轮评审（检测与基础遮挡）

日期：2026-03-11  
评审人：GitHub Copilot（GPT-5.3-Codex）

## 第1轮：需求覆盖与架构评审
### 评审目标
确认是否覆盖第4周目标：检测接口、黑框遮挡、capture->vision->render 管线、核心参数配置。

### 检查结果
- 检测接口已接入：`ITextDetector`、`TextDetection`（`include/subeclipse/detector.h`）。
- 简版检测器已实现：`SimpleTextDetector`（`src/vision/simple_text_detector.cpp`），能输出 ROI 局部文本候选框。
- 管线已建立：`CaptureVisionPipeline` + `SpscBlockingQueue`（`include/subeclipse/pipeline.h`、`src/app/pipeline.cpp`）。
- 渲染链路已接入：`OverlayWindow::draw(roi, mask_boxes)` 在 overlay 上填充黑框。
- 参数配置已接入：`detect_threshold`、`capture_fps`、`mask_style`（`config.h` + `config.cpp` + `configs/default.json`）。

### 结论
通过。

## 第2轮：构建与运行评审
### 评审目标
确认第4周改动可编译、可运行。

### 验证
- 执行：`cmake -S . -B build-debug -DCMAKE_BUILD_TYPE=Debug && cmake --build build-debug -j`
- 结果：构建成功（`subeclipse_core` 与 `subeclipse`）。
- 执行：`timeout 5s ./build-debug/subeclipse`
- 结果：程序可正常启动并进入主循环。

### 结论
通过。

## 第3轮：行为语义与质量评审
### 评审目标
验证“ROI 内遮挡、非文本透明”语义与容错行为。

### 检查结果
- 遮挡语义：仅绘制 `mask_boxes` 黑框，未对其他区域填充，满足“非文本保持透明”。
- 坐标语义：检测框使用 ROI 局部坐标，主循环映射回屏幕坐标后渲染，坐标链路正确。
- 管线生命周期：Space 进入运行态启动管线，暂停/重选时停止管线并清空遮挡框，状态收敛清晰。
- 参数容错：阈值与 FPS 在配置加载时已做边界修正，降低异常配置风险。

### 结论
通过。

## 最终结论
第4周 MVP 目标已达成：文本检测与基础黑框遮挡链路已跑通，可在当前架构上继续推进第5周的跟踪与抖动抑制。
