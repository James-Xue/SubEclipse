# 第3周代码三轮评审（X11 抓屏链路）

日期：2026-03-11  
评审人：GitHub Copilot（GPT-5.3-Codex）

## 第1轮：结构与需求匹配评审
### 评审目标
确认代码是否完整覆盖第3周任务：`IScreenCapture/X11Capture`、`XShmGetImage` 优先与 `XGetImage` 回退、ROI 局部抓屏、时间戳、重连与错误提示。

### 评审结果
- 新增抓屏接口与实现：`include/subeclipse/capture.h`、`src/platform/x11/x11_capture.cpp`。
- 抓屏策略正确：`grab()` 中优先走 `grab_with_shm`，失败后走 `grab_with_xgetimage`。
- ROI 局部抓屏正确：`set_roi` + `validate_and_clamp_roi` 处理非法/越界 ROI。
- 时间戳正确：成功抓帧后填充 `Frame.ts_ms`。
- 重连与错误提示具备：`ensure_connected` 节流重连，`warn_throttled` 节流日志。
- 主流程接入已打通：`main.cpp` 在运行态按固定间隔抓取 ROI 并输出统计。

### 结论
通过。

## 第2轮：构建与运行评审
### 评审目标
确认改动可编译并可运行。

### 验证
- 构建命令：
  - `cmake -S . -B build-debug -DCMAKE_BUILD_TYPE=Debug && cmake --build build-debug -j`
- 结果：构建成功（`subeclipse_core` 与 `subeclipse` 均通过）。
- 运行烟测：
  - `timeout 4s ./build-debug/subeclipse`
- 结果：程序正常启动进入主循环（超时退出为预期）。

### 结论
通过。

## 第3轮：容错、回退与可维护性评审
### 评审目标
验证异常路径与长期维护风险。

### 检查要点
- Display 断开处理：失败后断连并按 1s 节流重连，避免忙等。
- XShm 回退：`XShmCreateImage` / `XShmAttach` / `XShmGetImage` 任一失败均可回退到 `XGetImage`。
- 资源释放：共享内存与 X11 资源在 `release_shm_image` / `disconnect_display` 中集中回收。
- 日志节流：避免异常环境下日志刷屏影响调试体验。

### 结论
通过。

## 最终结论
第3周代码已达成“可用抓屏链路 + 可验证 + 可回退”的目标，可继续推进第4周检测与遮挡管线接入。
