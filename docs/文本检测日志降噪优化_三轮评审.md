# 文本检测日志降噪优化三轮评审

日期：2026-03-12  
评审人：GitHub Copilot（GPT-5.3-Codex）

## 变更范围
- `src/vision/simple_text_detector.cpp`

## 第1轮评审：策略与行为一致性
### 目标
确认日志从“高频明细”优化为“摘要优先”，且不改变检测算法行为。

### 检查结果
- 检测核心判定逻辑保持不变：
  - `gx + gy >= grad_threshold`
  - `density >= row_density_threshold`
  - `width < max(12, frame.width/20)` 过滤
  - `score >= threshold * 0.5` 保留
- 候选日志由逐条输出改为聚合计数：`candidate_count/width_reject_count/score_reject_count/accept_count`。
- 引入节流：
  - 摘要日志：首帧 + 每 20 帧一次（`[Detector][Summary]`）；
  - 采样 reject：按帧间隔控制；
  - 连续异常（无 band/无检测）：每 30 帧提示一次。

### 结论
通过。

## 第2轮评审：构建与诊断
### 验证命令
- `cmake -S . -B build-debug -DCMAKE_BUILD_TYPE=Debug`
- `cmake --build build-debug -j`

### 结果
- 构建成功：`subeclipse_core`、`subeclipse` 均通过。
- `src/vision/simple_text_detector.cpp`：无错误。

### 结论
通过。

## 第3轮评审：调试可用性与信息密度
### 目标
确认日志量显著下降，同时保留定位漏检所需信息。

### 检查结果
- 摘要日志包含关键定位字段：
  - 输入尺寸与阈值映射
  - `row_hits_mean/hit_rows`
  - `band_count/first_band_density`
  - `active_segments/candidate_count/accept_count`
  - `width_reject_count/score_reject_count`
  - `final_detection_count/no_band_streak/no_detection_streak`
- 即时告警保留关键问题：`invalid frame`、`detection_count_capped`、连续无结果。
- 预计每秒日志量显著下降，且调试信息更聚焦。

### 结论
通过。

## 最终结论
本次“文本检测日志降噪优化”完成并通过三轮评审。当前版本更适合长时间调试：日志更少、信号更强、定位路径更清晰。